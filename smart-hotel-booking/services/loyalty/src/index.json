const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.send('Loyalty service is running!');
});

app.listen(port, () => {
  console.log(`Loyalty service listening at http://localhost:${port}`);
});
